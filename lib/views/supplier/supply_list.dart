import 'package:flutter/material.dart';

class SupplyListView extends StatelessWidget {
  const SupplyListView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
